# Get updates

**DataPass: Get Updates** fetches every repository of the project and fast-forwards the ones that can be, safely:

- a repository with local changes, or whose branch has diverged, is left alone and listed;
- nothing is merged, rebased or pushed for you.

The **Git** view shows what needs you: changes to commit, branches to push, pull requests with their CI.
