# Night notes

Release notes of packages merged but not yet released, one file per package (`<version>-<id>.md` or `v1-<id>.md`). The release coder folds them into the top of [IMPLEMENTATION_STATUS.md](../../../IMPLEMENTATION_STATUS.md) and removes them. Empty right after a release.
