# DiagramCloud follow-up — contract alignment only

Keep DiagramCloud independent. Read 05_DIAGRAMCLOUD_CONTRACT.md in this folder before implementing the bridge.

The existing Project schemaVersion 1 is strict and is not a DataPass manifest. Map a selected, reviewed DataPass architecture view to native node/view/edge/source/evidence objects, preserve stable IDs through a sidecar, and report unsupported/lost fields. Layout and semantic revisions differ. The input's provenance/status must not be collapsed into illustrative running/complete animations.

The inspected schema defaults visibility to public and requires a public root view. Until a private-authoring path is qualified, accept only an explicitly reviewed redacted projection or keep the private interchange file unimported. Do not treat a public flag as permission to publish client materials. Test export paths as well as the canvas.

No confidential JSON or signed storage links in URL query strings. Initial integration is a local file/clipboard exchange plus Open DiagramCloud, not automatic cloud synchronization or a second full app inside Galaxy.

Changes returned to DataPass are version-bound proposals. They cannot deploy infrastructure, rewrite FOIL authority records or publish content. BigQuery/Kafka in sample content is not the latest FOIL R0 architecture. Keep learning/portfolio reconstruction visibly separate from active engineering.

Mongoku remains deferred: a future sanitized project-summary consumer, not another task or architecture authority. This follow-up does not modify either repository or implement a live bridge.
