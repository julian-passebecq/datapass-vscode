# PLAN — DataPass Control Plane (datapass-vscode)

Kept by `ARCHI DataPass N` (docs/roles/archi.md in claude-control). Lean on purpose: one screen per version. Estimates are orders of magnitude; fill **Actual** when a package merges (GALAXY Contrôle compares them every week).

Updated: 2026-09-26 (after 0.24.0) by ARCHI DataPass 1 · Sources: [v3/11_FOIL_MCP_REVIEW.md](v3/11_FOIL_MCP_REVIEW.md) (FOIL review of 0.24, decisions D-19…D-28, 0.26/0.27 packages), [v3/10_GLOBAL_IMPROVEMENT_PLAN.md](v3/10_GLOBAL_IMPROVEMENT_PLAN.md) (decisions D-01…D-18, §3 features by version), [v3/08_TOOLKIT_AND_AGENTS.md](v3/08_TOOLKIT_AND_AGENTS.md) §5.1, 5.2, 5.4 (toolkit catalogue), [v3/09_AI_MODES_WORK_ORDERS_GIT.md](v3/09_AI_MODES_WORK_ORDERS_GIT.md) §12–13.1 (AI-3, AI-4, pass order), main `3b1635b` (0.24.0 released).

## Now

Kept by ASSISTANT ARCHI DataPass 2 (decisions: ARCHI DataPass 2). Released: **0.26.0** (commit `c78f01f`, PR #72). On main for 0.27: K2 (#71), V1-STAB (#81), QA-0 (#76), QA-1 (#79).

Supplier/client rule (Julian, 2026-09-26): we write the extension, common and hub. A client's bridge (link repository) we may draft first; the client AI then reviews and maintains it. A client's native code (foil-study, the fake client's native repos) we never write.

| Lane | Package | Coder | State |
|---|---|---|---|
| Stabilise | V1-PERF (V1-STAB merged #81; `windowFlows` flake follow-up #84) | TAMPON 25 | running |
| Codex | QA-2 · QA-3 common/testing + vsixtest seed (docs #82) · HUB-1 hub/common seeds + "Working with a client" (QA-1 follow-up #83) | M 1 · TAMPON 22 · 18 | running; HUB-1 waits for Julian's OK to write the public common repo |
| Next | V1-PAL (after PERF, QA-2) → 1.0.0-rc (also needs HUB-1) · CQ after HUB-1 | M 2 · ARCHI 2 · TAMPON 24 | queued |
| FOIL AI (client) | Codex Wind Lab, then FOIL (V1-FOIL-A depends on it and on Julian's FOIL #5/#6 + foil-study#1 reviews) | — | client side |

## Versions

"V1 / V2 / V3" are the feature tiers of plan 10 §3, not extension releases (current release: **0.26.0**; next: **0.27.0**, then **1.0.0-rc**).

| Version | Goal (one line) | Useful? | Status |
|---|---|---|---|
| V1 — up to 1.0.0 | The daily loop on real projects: bridge repository, architecture first, modes, context from any file, format checks, file versions, variants, toolkit catalogue, AI work orders | yes: it is what Julian and FOIL use now | in progress: 0.22.0, 0.23.0 (variants + toolkit catalogue) and 0.24.0 (AI-3) released; 0.25.0 (selected variant, repository layout) and 0.26.0 (pilot without sign-in, Open a Client Project, MCP and cost repairs, Mongoku removed) released; V1 packages of [ROADMAP.md](ROADMAP.md) running toward 1.0.0-rc (K2 merged after the 0.26.0 cut: ships in 0.27.0) |
| V2 | Bridge recommends a mode (manifest v6 `presentation`), presets from the hub, check plan / evidence rows, coordinated change sets shown, cost lines linked to toolkit ids, Python syntax check, variants on another branch/tag, side-by-side variant diff, company-level global view, Remote-SSH/WSL, Readiness rows filtered by the selected variant (from 0.25.0; V1-STAB may do it if small) | yes, after 1.0: each item comes from a real gap seen in V1 | later (J3, J5 recommended "yes, in V2") |
| V3 | Recipes as agent skills, read-only MCP server, Pilot stages 2–3, FOIL producer routes, diagram editing | no for now: depends on V1 used for real and on FOIL | parked |

Cut (plan 10 §1.5, still true): no knowledge database or price scraper, no virtual mega-repository or custom file browser, no compiler per provider, no DataPass files in native repositories, no second FOIL backlog, no Mongoku features (frozen).

## Packages (0.23.0 — toolkit catalogue)

Common rules (as in 0.22): each package in its own worktree from up-to-date main, branch `claude/0.23-<id>-<slug>`, `npm run verify` green, PR merged on green CI (never `gh pr merge --auto` in this repository: no required checks). **No version bump**, no edits to `CHANGELOG.md`, `IMPLEMENTATION_STATUS.md`, `CLAUDE.md`, `handoff/V3_HANDOFF.md`; release notes go to `handoff/v3/night/0.23-<id>.md`. In `package.json`, `src/extension.ts` and `tests/integration/suite.ts` add only your own entries and resolve those three by union when rebasing. The contract between T1, T2, T3 and T4 is the JSON shape of 08 §5.1 (tool) and §5.2 (recipe), wrapped in `{ "format": "datapass.toolkit", "version": 1, "requires"?: { "datapass": ">=0.23.0" }, "tools"|"recipe"…, "datapassRequests"?: [{ "title", "why", "example" }] }`; T1 owns the schema and may tighten it, T2 then adapts its data before merging.

| Id | Package | Size | Estimate (h · M tokens) | Model · effort | Owned files | Acceptance tests | Coder | Status | Actual (h · M tokens) |
|---|---|---|---|---|---|---|---|---|---|
| T1 | Toolkit core: format, schema, hub reader, built-in baseline, merge, "Needs a newer DataPass" data | L | 3–5 h · 40–90 | Opus 5.5 · high | `src/core/toolkit/**`, `schemas/datapass-toolkit.schema.json`, `resources/toolkit/**` (built-in baseline: the official tools and the probes of `src/core/capabilities/tools.ts`, which stays the probe layer and is only read), `tests/toolkit.test.ts` | Unit: tools.json and a recipe validate; unknown field rejected with a message; `requires.datapass` newer than the running version → file shown as "needs DataPass ≥ x", its entries skipped, never guessed; format version newer → same; hub entry overriding the baseline marked "changed by the hub"; a hub entry naming an unknown probe id is refused; install commands are data only (a test that `src/core/toolkit/**` imports nothing from `exec`/`child_process`); `datapassRequests` listed; hub absent → baseline only; recipe step `capability` resolved against the registry or flagged | toolkit session | merged (#43) | ≈ 9 h together (T1–T4, one session started on 0.20.0) · not split per package |
| T2 | Toolkit content: hub example data (Fabric + Power BI first, then Databricks, Azure), recipes incl. the Copy Job bulk edit (08 §4), guide page and preparation contract | M | 2–3 h · 25–50 | Opus 5.5 · medium | `examples/v3/hub/.datapass/toolkit/**`, `examples/v3/hub/README.md`, `docs/guide/09_TOOLKIT.md`, `docs/guide/README.md` (one line), `docs/PREPARING_A_PROJECT.md` (new toolkit section only) | Every file validates with T1's validator (after T1 merges: rebase, run it, fix the data); each tool has `verified.on` and links checked on the day; each recipe names existing tool ids; the guide explains who updates the hub (ChatGPT via the JSON exchange or an agent PR), what needs a newer DataPass, and `datapassRequests` | toolkit session | merged (#43) | ≈ 9 h together (T1–T4, one session started on 0.20.0) · not split per package |
| T4 | Board `recipe` field: board items may name `{ "recipe": "<id>", "route"?: "<id>" }`; validation (unknown id = warning when the toolkit is loaded, never an error that blocks the board); AI board pack asks for recipe ids when relevant | S | 1–2 h · 15–30 | Opus 5.5 · medium | `src/core/project/board.ts`, `src/core/project/boardPack.ts`, `schemas/datapass-board.schema.json`, `tests/board.test.ts` | Unit: a card with a recipe round-trips (moving a card still rewrites only `status`); old boards without `recipe` unchanged; unknown recipe id → warning; the pack text mentions recipes only when the toolkit has some. Board desktop flows still pass | toolkit session | merged (#43) | ≈ 9 h together (T1–T4, one session started on 0.20.0) · not split per package |
| T3 | Toolkit surfaces (after T1): tools and recipes where they are needed — Details (a component's provider → tools, recipes), Options comparison (tools per option), board card (recipe steps, from T4's field), Readiness *Tools & versions* links to catalogue entries; a Toolkit section with "Needs a newer DataPass" and files skipped; surface ids `project.toolkit`, `badge.hubChanged` added to presets (DataPass+; add, never rename) | L | 3–4 h · 40–80 | Opus 5.5 · high | `src/views/{workbenchHtml,workbenchState,projectTree}.ts` (toolkit parts only), `src/webview/workbench.ts` (toolkit parts), `src/work/toolkitCommands.ts`, `src/core/experience/surfaces.ts` + `resources/experience/presets.json` (new ids only), `tests/integration/toolkitFlows.ts` | Desktop flow on `examples/v3/hub` + `sales-bi`: Details of a Fabric component lists Fabric Studio and the `fab` route with its steps; a board card with a recipe shows its steps; a hub file requiring a newer DataPass appears under "Needs a newer DataPass" and its entries are skipped; Vanilla hides the Toolkit section, DataPass shows it; nothing is installed or run (copy only); no file written in any repository | toolkit session | merged (#43) | ≈ 9 h together (T1–T4, one session started on 0.20.0) · not split per package |
| T5 | Testlab 9 for Julian: modes, context from any file, format checks, file versions, variants, toolkit (offline, ≈ 25 min), then run it yourself with the desktop harness and Playwright where possible | M | 2–3 h · 20–40 | Opus 5.5 · medium | `D:\PROJ\datapass-testlab\9-modes-contexte-versions\**` (outside the repo), `D:\PROJ\datapass-testlab\LISEZ-MOI-TESTS.md` (one row), `handoff/v3/04_NEXT_PASSES.md` (a "B6. Testlab project 9" section only) | `setup.ps1` builds the fixture from scratch twice without error; `LISEZ-MOI.md` in French, step by step, each step with what Julian should see; every step not needing Julian's eyes or hardware run once by the coder and marked done; the toolkit steps added after T3 merges (or marked "0.23, after install") | TAMPON 10 | merged (#45) | ≈ 2.5 h · n/a |
| R | Release 0.23.0: bump, `CHANGELOG.md`, top of `IMPLEMENTATION_STATUS.md`, `CLAUDE.md` current-source line, `04_NEXT_PASSES.md` "Done in 0.23.0", fold `handoff/v3/night/0.23-*.md` (and G's notes) in, full desktop suite, VSIX built and installed, todo.md row for Julian | S | 1 h · 10–20 | Opus 5.5 · medium | the shared release files above | `npm run verify` + full desktop suite green; VSIX installs; PR merged on green | toolkit session or a TAMPON | merged (release PR) | ≈ 1 h |

Size guide: **S** < 1 h, one area · **M** 1–3 h, a few files · **L** > 3 h or hard (effort high, or split it).

**Reassigned (2026-09-26 10:20):** T1–T4 are built by the session already running the toolkit catalogue pass ("DataPass: build the toolkit catalogue pass (0.21.0)", started on 0.20.0, work uncommitted at staffing time); it rebases on main and ships them as 0.23.0 against the acceptance above. The TAMPONs sent on T1/T2/T4 were stopped before any commit. T5 runs on TAMPON 10. R: the toolkit session, or a TAMPON if it hands off.

## Merge order

T4 → T1 → T2 → T3 → R (0.23.0). T5 merges its one repository line whenever green (docs only). Each coder merges on green CI, then tells the next one (send_message); a coder whose predecessor has not merged yet rebases on main before merging.

## Packages (0.25.0 — switchable variants and repository layout; Julian's FOIL answers, 2026-09-26)

Inputs (relayed by SUPERVISOR 2 from foil-v1-vscode-datapass PR #5, `docs/DATAPASS_PREPARATION.md` §5): **Q1** the bridge stays a pure, light bridge (links + DataPass JSON), ideally invisible in what the client and its auditor receive; code lives in native repositories, his lean is one repository per sub-project (a monorepo with sub-folders is acceptable); the guide says "we prefer X, you may also do Y". **Q5** V1 orchestration = three switchable variants (A direct script, B Blob event + Function, C ADF), switched in a few clicks without reconfiguring: active folders in the tree, what is sent to the AI, prices and scenarios prepared upstream by the client AI. **Q4** work orders on FOIL: yes, but Julian says when (standby). **Q6** vocabulary: the FOIL AI decides. **Q2/Q3** (Azure names, the two VMs): open, Julian's data. The Microsoft Fabric MCP server goes to the toolkit hub as a catalogue entry once the FOIL AI's report arrives (data, no code).

Same common rules as 0.23 (no version bump in packages, notes in `handoff/v3/night/0.25-<id>.md`, union on the three shared files, never `--auto`).

| Id | Package | Size | Estimate (h · M tokens) | Model · effort | Owned files | Acceptance tests | Coder | Status | Actual (h · M tokens) |
|---|---|---|---|---|---|---|---|---|---|
| V-A | **Active variant**: a machine-local "working scenario" per project (no file written; *Record decision* stays the committed path) chosen from a status-bar item and the Options/Architecture panel in 1–2 clicks; it drives the Selected-architecture tree, Details, the diagram preview, Readiness / toolchain rows of the variant's components, *Copy Context for My AI* and the options/work-order packs (they name the active variant and its coding state). Public example `examples/v3/doc-pipeline` (PDF → storage → processing, options A direct script / B Blob event + Function / C ADF, scenarios with dated per-currency prices, planned files for B and C). Guide page `docs/guide/10_SWITCHING_VARIANTS.md` | M | 1.5–3 h · 15–35 | Opus 5.5 · medium | new `src/core/project/activeVariant.ts`, `src/work/activeVariantCommands.ts`, the active-variant hooks in `src/views/{projectTree,workbenchState,workbenchHtml}.ts`, `src/webview/workbench.ts`, `src/core/exchange/fileContext.ts` + `src/core/project/optionsReport.ts` (pack header lines only), surface id `status.activeVariant` (Standard+), `examples/v3/doc-pipeline/**`, `docs/guide/10_SWITCHING_VARIANTS.md`, `docs/guide/README.md` (one line), tests | Unit: switching A→B→C changes the filtered file set and the pack header, writes nothing (git status clean on the fixture), survives reload (machine state), falls back to "current" when the scenario disappears; example validates. Desktop flow on doc-pipeline: switch in the status bar, tree and Details follow, Copy Context names variant B | TAMPON 9 | merged (#54) | ≈ 1.3 h · ≈ 7 M |
| R-L | **Repository layout contract** (Q1): guide and preparation contract say "we prefer one native repository per sub-project (GitHub or Azure DevOps, one Azure DevOps project with several repositories), you may also use one repository with sub-folders (`path`)"; the bridge holds only links and DataPass JSON, never code, and is not part of what the client or auditor receives; checked against current Azure DevOps / GitHub guidance (cite, dated) | S | 0.5–1 h · 5–15 | Opus 5.5 · medium | `docs/guide/02_WHAT_THE_AI_PREPARES.md` (repositories section only), `docs/PREPARING_A_PROJECT.md` (repositories rows and a "Repository layout" subsection), `docs/guide/01_OVERVIEW.md` (bridge paragraph only), `docs/guide/06_PROMPT_FOR_THE_CLIENT_AI.md` (one rule) | Docs PR merged; sources cited with the check date; wording "we prefer … you may also …"; no FOIL-private detail in this public repository | TAMPON 10 | merged (#52) | |
| R2 | Release 0.25.0 (as R of 0.23) | S | 0.5 h · 5–10 | Opus 5.5 · medium | release files | full desktop suite, VSIX installed, todo.md row | TAMPON 9 (after V-A) | **released 0.25.0** (#65): selected variant (`status.selectedVariant`, `datapass.setSelectedVariant`), preview/test/activate wording, files-present badges; VSIX installed | ≈ 1.2 h · ≈ 6 M |
| AI-4a | Pilot stage 1 without Azure sign-in: pilot folder with the decided Claude/Codex settings, launch profiles, requests/responses and Pilot card, `datapass.pilot.enabled`, stubs only (0.26) | M | 2–3 h · 30–50 | Opus 5.5 · medium | `src/core/pilot/**`, `src/work/pilot*.ts`, pilot parts of `workOrders.ts` / `workOrderCommands.ts` / `aiExchange*.ts`, pilot schemas, `tests/pilot*` | the 7 tests of the decision + request validation + desktop flow with a stub agent | TAMPON 11 (moved to the 0.26 table) | see 0.26 | |
| FB | FOIL bridge preparation in `foil-v1-vscode-datapass` with Q1/Q5 (repositories per sub-project, light bridge, options A/B/C), Q6 left to the FOIL AI, Q2/Q3 placeholders; draft PR stacked on its PR #5, **not merged** (work project: Julian reviews) | M | 1.5–3 h · 15–35 | Opus 5.5 · medium | that repository only | every `.datapass/*.json` valid with DataPass's validators; no code, no credentials, no guessed Azure names | TAMPON 1 | done: draft PR #6 in foil-v1-vscode-datapass (Julian merges) | |

Merge order: R-L (docs, any time) → V-A → R2. AI-4a moved to 0.26 below. FB is merged by Julian. Rule from SUPERVISOR 2 (Julian): always keep one package running that needs nothing from Julian; draft PR after ~2 h of work.

## Packages (0.26.0 — pilot without sign-in, FOIL MCP review repairs)

Source: [v3/11_FOIL_MCP_REVIEW.md](v3/11_FOIL_MCP_REVIEW.md) (findings R-01…R-12, decisions D-19…D-28, §4 packages). Same common rules (no version bump, notes in `handoff/v3/night/0.26-<id>.md`, union on the three shared files, never `--auto`). Coordination and PLAN.md rows: ASSISTANT ARCHI DataPass 1; design questions: ARCHI DataPass 1.

| Id | Package | Size | Estimate (h · M tokens) | Model · effort | Owned files | Acceptance tests | Coder | Status | Actual (h · M tokens) |
|---|---|---|---|---|---|---|---|---|---|
| AI-4a | Pilot stage 1 without Azure sign-in: pilot folder with the decided Claude/Codex settings ([brief](briefs/2026-09-26-ai4-pilot-allowlist.md)), launch profiles, requests/responses and Pilot card, `datapass.pilot.enabled`, stubs only | M | 2–3 h · 30–50 | Opus 5.5 · medium | `src/core/pilot/**`, `src/work/pilot*.ts`, pilot parts of `workOrders.ts` / `workOrderCommands.ts` / `aiExchange*.ts`, pilot schemas, `tests/pilot*` | the 7 tests of the decision + request validation + desktop flow with a stub agent | TAMPON 11 | merged (#55); Julian's 10-min pilot qualification after his read-only `az` sign-in (todo.md) | ≈ 2 h · ≈ 0.36 M (context) |
| M1 | MCP writer repair (R-01, R-02, R-03 label): rewriting `.vscode/mcp.json` keeps every field of other servers (`env`, `type`, `cwd`, `envFile`, `inputs`); discriminated stdio / http servers; unknown dialect (`mcpServers`) or comments refused, never emptied; `ws.mcp` relabelled "registration file present" (not connected) | S–M | 1–2 h · 15–30 | Opus 5.5 · medium | `src/core/mcp.ts`, the `ws.mcp` entry of `src/core/capabilities/tools.ts`, MCP call sites in `src/core/actions.ts` / `src/work/optionsCommands.ts`, `tests/mcp.test.ts` | regression tests reproducing R-01/R-02 first (red on main), then green; a round-trip keeps unknown fields byte-for-byte in meaning; http server accepted; unknown dialect refused with a message and the file untouched | TAMPON 14 | merged (#57); `optionsCommands.ts` now exports `showProposalDiff`, `writeProjectFile` takes `Pick<WorkSession,"root">`; minor items in `night/0.26-m1.md` | ≈ 1 h · ≈ 10 M (estimated) |
| K1 | Toolkit MCP knowledge (R-07), data only: the baseline distinguishes Fabric Core (remote) / local Fabric MCP / Fabric IQ / Power BI Authoring MCP / the Microsoft `powerbi-authoring` plugin / Azure MCP; corrects FabricStudio, data-goblin, fabric-toolbox, semantic-link-labs; dated sources | S–M | 1–2 h · 10–25 | Opus 5.5 · medium | `resources/toolkit/**`, `examples/v3/hub/.datapass/toolkit/**`, `docs/guide/09_TOOLKIT.md` (MCP section) | every file validates with the toolkit validator; each entry has `verified.on` and checked links; no schema change unless a `datapassRequests` entry proves the need | TAMPON 15 | merged (#60): 6 MCP/plugin entries verified 2026-09-26, 6 corrections, read-only MCP recipe, 2 `datapassRequests`, guide 09 MCP section | ≈ 0.8 h · ≈ 0.2 M (session counter) |
| X1 | Remove Mongoku from the visible scope (R-08): no Mongoku surface, command, companion or board wording; old configurations that name it still load (ignored with a note) | M | 1.5–3 h · 15–35 | Opus 5.5 · medium | Mongoku parts of `package.json`, `src/extension.ts`, `src/core/{companions,modules,projectManifestModel}`, `src/core/project/board*.ts`, `src/core/readiness/readiness.ts`, `src/views/workTree.ts`, `src/webview/workbench.ts`, `src/work/{board,companion}Commands.ts`, `src/work/session.ts`, tests | an old manifest / board naming Mongoku loads without error; no visible "Mongoku" string in contributed commands, views or webviews; existing tests green | TAMPON 16 | merged (#63): Mongoku commands, URL setting, URI handler, rows and module removed; old `modules.mongoku` / `companions.*` accepted and ignored | ≈ 2.5 h · ≈ 18 M together with R3 (estimated) |
| C1 | Cost basis in options.json (D-24): cost lines gain `shared` (same key across options counts once in combined totals; differing figures → unpriced + message) and `use` (`learning-only` → note, never hidden); options.json stays version "1" | M | 1.5–2.5 h · 15–30 | Opus 5.5 · medium | per [briefs/2026-09-26-cost-basis.md](briefs/2026-09-26-cost-basis.md) "## Decision": `schemas/datapass-options.schema.json` (cost line), `src/core/project/costs.ts`, cost parts of `options.ts` / `optionsReport.ts` and the Options webview, `examples/v3/doc-pipeline/.datapass/options.json`, guide 02 cost section, PREPARING_A_PROJECT options rows, `tests/costs.test.ts` | the Decision's acceptance (shared counted once, disagreeing figures unpriced, currencies apart, learning-only flagged, 0.22 regression); schema refuses bad key / `use` | TAMPON 19 | merged (#61), ships in 0.26.0; doc-pipeline gains option D (Databricks Free Edition, learning-only); one extra line in `workbenchState.ts` (cost type) | ≈ 0.55 h · ≈ 3 M (estimated) |
| R3 | Release 0.26.0 (as R of 0.23) after AI-4a, M1, K1, X1 (and C1 if ready) are merged | S | 0.5–1 h · 5–15 | Opus 5.5 · medium | release files | `npm run verify` + full desktop suite green; VSIX installed; todo.md row for Julian | TAMPON 16 (right after X1) | **released 0.26.0** (#72): AI-4a, M1, K1, C1, X1 + first V1 items E1, V1-ON, V1-P1, V1-DOC, V1-T10; VSIX installed; todo.md row | see X1 |

FOIL side (Julian reviews and merges; DataPass sessions never merge them):

| Id | Package | Repository | Coder | Status | Actual |
|---|---|---|---|---|---|
| FB2 | Bridge Q6 (vocabulary from the FOIL AI) + reconciliation with 11 (R-09: manifest version, producer, preparation file) on draft PR #6 | foil-v1-vscode-datapass | TAMPON 17 | done: draft PR #6 +2 commits (Q6 vocabulary, study-core + A/B/C adapters, R3 fixes, board.json dropped per D-10), validated on main `299e222`; Q2/Q3 still open; **Julian reviews and merges** | ≈ 0.5 h · ≈ 8–10 M (estimated from context) |
| S1 | foil-study common core + route A, PR for Julian's review | foil-study | TAMPON 18 | done: [foil-study#1](https://github.com/julian-passebecq/foil-study/pull/1), CI green, 38 tests; B/C README contracts only; 3 choices flagged for Julian; **Julian reviews and merges** | ≈ 0.3 h · ≈ 0.16 M |

Merge order (0.26): AI-4a, M1, K1 in any order as each is green (rebase on main first) → X1 last (it touches shared files) → R3 release 0.26.0; C1 joins if merged before R3. Next (0.27, from 11 §4): P1 pack stamps, E1 evidence chain, T10 testlab 10 — started before 0.26.0 ships (Julian lifted the 85 % weekly cap for DataPass, relayed by ARCHI DataPass 1): table below.

## Packages (0.27.0 — stamps, evidence, testlab 10; P1 and T10 are tracked as V1-P1 / V1-T10 below)

From [v3/11_FOIL_MCP_REVIEW.md](v3/11_FOIL_MCP_REVIEW.md) §3–4. Same common rules; notes in `handoff/v3/night/0.27-<id>.md`. More packages will come from `handoff/ROADMAP.md` (ARCHI DataPass 1, in progress).

| Id | Package | Size | Estimate (h · M tokens) | Model · effort | Owned files | Acceptance tests | Coder | Status | Actual (h · M tokens) |
|---|---|---|---|---|---|---|---|---|---|
| P1 (= V1-P1) | D-23 stamps: packs and work orders record selected variant, environment, bridge revision; a copied pack goes stale when the selection changes; launching a work order stamped for another variant asks first; unstamped old orders still load | S–M | 1–2 h · 15–30 | Opus 5.5 · medium | stamp parts of `src/core/workOrders/{builder,format,launch}.ts`, pack header of `fileContext.ts` / `optionsReport.ts`, `src/core/project/aiExchange.ts`, `src/views/aiExchange{,Html,State}.ts`, `src/work/workOrderCommands.ts`, work-order schema (additive), `tests/workOrders.test.ts`, `tests/fileContext.test.ts`, `tests/integration/workOrderFlows.ts` | unit: stamp written; variant change → stale with reason; other-variant launch → confirmation; old order loads; desktop flow on doc-pipeline (B → C). Rebase after AI-4a (#55) | TAMPON 21 | merged (#66), see V1-P1 | ≈ 1.1 h · ≈ 25 M (estimated) |
| E1 | D-22 evidence chain: known → installed → registered → connected → authenticated → authorized → verified, each `observed` (source, time) or `unknown` with a reason, in Readiness and Details; receipts separate CLI exit / CI / deployed / runtime / scientific validity; an agent's claim stays "asserted" | M | 1.5–3 h · 20–40 | Opus 5.5 · medium | new `src/core/evidence/**`, evidence parts of `src/core/capabilities/{tools,registry,probe}.ts`, `src/core/toolchain/connections.ts`, `src/core/readiness/readiness.ts`, Details parts of `src/views/workbench{Html,State}.ts`, receipt fields of `src/core/workOrders/status.ts`, `tests/evidence.test.ts` | unit: links default unknown; registration ≠ connected; no credential file read; assertion-only receipt → "asserted, not verified"; old receipts load; desktop flow: Readiness chain for `az` and the Fabric MCP entry. Rebase after M1 (#57) | TAMPON 22 | merged (#67), in V1; left: a prompt row in `builder.ts` (P1) so agents fill `field/tool/scope/input`; "operation verified" never observed yet (no DataPass-run check) | ≈ 0.6 h · ≈ 20–25 M (estimated) |
| T10 (= V1-T10) | D-28 testlab 10 on `doc-pipeline`: the review's acceptance journeys (DataPass disabled, preview A/B/C with the right packs, missing clone, unverified remote, dirty file, unsupported tool, variant never run, MCP card unknown states), run by the coder; Julian's visual steps in todo.md | M | 1.5–2.5 h · 15–30 | Opus 5.5 · medium | `D:\PROJ\datapass-testlab\10-*\**`, `LISEZ-MOI-TESTS.md` (one row), `handoff/v3/04_NEXT_PASSES.md` (B7 section) | `setup.ps1` twice clean; each step with what to see; steps not needing Julian run and marked | after P1 and E1 merge | todo | |
| R4 | Release 0.27.0 | S | 0.5 h · 5–10 | Opus 5.5 · medium | release files | as R3 | — | todo | |

## Packages (V1 → 1.0.0, from [ROADMAP.md](ROADMAP.md) §1.3 and the Codex test mode [v3/12_CODEX_TEST_MODE.md](v3/12_CODEX_TEST_MODE.md) §8)

Codex modes (Julian, 2026-09-26, ARCHI decisions): **vsixtest** first (Codex tests the released VSIX across several clients in `codex-datapass-vsixtest`, answers V01–V19), then **auto** (client journeys). No GitHub release asset: the VSIX is a local path built from the release tag; a public GitHub release would need Julian.

Copied from ROADMAP §1.3 (what to code and acceptance in full there). Lanes (§1.5): A now = V1-ON, V1-DOC, V1-T10 · B after AI-4a = V1-P1 · C after 0.26.0 = V1-STAB then V1-PERF · D client, after Julian's reviews = V1-FOIL-A · last = V1-PAL → 1.0.0-rc → Julian's check → 1.0.0. Same common rules; notes in `handoff/v3/night/v1-<id>.md`. Julian lifted the weekly budget cap for DataPass (2026-09-26, via ARCHI DataPass 1).

| Id | Package | Size · effort | Owned files | Acceptance tests | Coder | Status | Actual (h · M tokens) |
|---|---|---|---|---|---|---|---|
| V1-ON | Open a client project | M · medium | new `src/work/openClientProject.ts`, `src/core/project/cloneplan.ts`, `resources/walkthrough/**`, its package.json entries | Unit: clone plan from a manifest (planned skipped, existing clone located by remote identity incl. SSH/https equivalence, Azure DevOps forms); desktop flow on a local "GitHub on disk" fixture with 3 repositories: one command → workspace with 3 roots open, Architecture shown; second run clones nothing | TAMPON 20 | merged (#68): command, walkthrough, welcome entries, DEMARRER paragraph; 12 unit + 2 desktop fixtures | ≈ 1.5 h · ≈ 0.21 M (context) |
| V1-DOC | Docs you can read | M · medium (docs-writer) | those files | Links checked; CLAUDE.md < 60 lines; nothing deleted, only moved with redirects | TAMPON 18 | merged (#64): DEMARRER.md, handoff/CURRENT.md, 49 files to handoff/archive/, README top; CLAUDE.md already 22 lines (left as is, Julian asked about trimming); DEMARRER's "0.27+" note to update when V1-ON ships | ≈ 0.55 h · ≈ 0.07 M |
| V1-T10 | Testlab 10 = acceptance journeys (D-28) | M · medium | testlab folder, `04_NEXT_PASSES.md` B7 | setup twice clean; coder runs every non-visual step; Julian's visual steps in todo.md | TAMPON 23 | merged (#69): `D:\PROJ\datapass-testlab\10-parcours-client`, 10/10 journeys on 0.25.0 and main; V1-ON (#68) and E1 card steps marked 0.27+; Julian's visual steps in todo.md (≈ 20 min) | ≈ 1.2 h · < 10 M (estimated) |
| V1-P1 | Stamped packs and orders (D-23) | S–M · medium | `src/core/exchange/stamp.ts`, stamp lines in the pack builders, the order `stamp` field (schema additive), the AI-view stale badge | Unit: stamp content; stale after variant / HEAD change; order launch confirmation. Desktop: switch A→B, pack shows stale | TAMPON 21 | merged (#66): `src/core/exchange/stamp.ts`, optional `stamp` in order.json, stale packs and Pilot cards, Keep/Rebuild/Cancel on other-variant launch, E1 result-fields prompt row; left: board-card, preparation and Workbench pack headers not stamped yet | ≈ 1.1 h · ≈ 25 M (estimated) |
| E1 | Evidence chain (D-22), moved into V1 — see the 0.27 table above | M · medium | see 0.27 table | see 0.27 table | TAMPON 22 | GO | |
| K2 | Microsoft MCP servers in the baseline (K1 leftovers): move `mcp.fabric-core`, `mcp.fabric-local`, `mcp.fabric-iq`, `mcp.powerbi-authoring-hosted`, `plugin.powerbi-authoring`, `mcp.azure` from the example hub into the built-in baseline (and the tool registry, since `baselineTools()` shows registry ids only); K1's two `datapassRequests`: MCP `transport` / `endpoint` / `hosts` (additive optional, format stays 1) and a `sends-to-model` side effect, shown in Details and the Toolkit view | S · medium | the tool registry, the toolkit baseline, the toolkit schema and parser, the Toolkit view, guide 09 MCP section | unit: the six ids in `baselineTools()` with no hub; new fields validate and display; unknown transport refused | TAMPON 15 (reused, did K1) | merged (#71): six ids built in (kind `mcp-server`), removed from the example hub; `transport` / `endpoint` / `hosts` / `sends-to-model` in the toolkit format, Toolkit view and Details | ≈ 1.5 h (≈ 50 min waiting on red main) · ≈ 0.1 M (context) |
| QA-0 | Verify how Codex uses a VSIX (investigation, no product code; priority 1) | S · medium | `handoff/briefs/2026-09-27-codex-procedure.md` | answers each 12 §4.6 question with a source (official Codex docs); throwaway run on this PC: Codex installs the VSIX in an isolated profile, opens doc-pipeline, clicks one view, screenshot; exact commands and failures | TAMPON 19 (reused) | merged (#76): host = Codex desktop app only (sandbox can't launch VS Code; `codex exec` Computer Use sees no apps); isolated-profile install verified; in-app run needs Julian's approvals and foreground (todo.md, ≈ 10 min) | ≈ 1.2 h · ≈ 5 M (estimated) |
| QA-1 | Formats + `qa:prepare` (revised 12: client journeys, no API battery runner; priority 1) | S–M · medium | `src/qa/**`, `scripts/qa/prepare.ts`, `tests/qa*.test.ts`, `tests/fixtures/qa/**`, schemas `datapass-codex-tests` (`purpose` app/client, `workspaces[]`) / `datapass-test-journey` (`kind` app/client) / `datapass-qa-report` (+ `purpose`, `answers[]`), `package.json` script `qa:prepare`, `docs/guide/11_CODEX_TESTS.md` | validators incl. negatives (path escape, http remote, unknown feature tag, bad outcome, oversized fields); fixture run root: VSIX (local path, sha256 recorded) in the isolated extensions dir, one `.code-workspace` per client for app, workspace file, valid `run.json`, exit 0; wrong remote → exit 2 | TAMPON 23 (reused; restarted after the re-scope) | merged (#79): formats + schemas + `npm run qa:prepare`, CI job on the packaged VSIX; follow-up #83 (Codex procedure addendum: `host: codex-desktop`, preconditions, known leaks, screenshot pattern, `--commit`/`--launch`; app workspaces `{id,title,bridge,repositories}` + `path`) | n/a |
| QA-2 | The mode in DataPass: Codex tests section, `qa-run` order kind, *Hand to Codex* | M · medium | `src/work/codexTests.ts`, `qa-run` kind (additive), Work orders view section, surface `ai.codexTests`, setting `datapass.codexTests.autoRepository` | prompt from the auto file; receipt stamp match; section lists journeys and the last report; hidden in Standard | TAMPON DATAPASSVSCODE - M 1 | GO (with QA-0 findings: Codex desktop app only, VS Code launched outside the sandbox, foreground + Code.exe approval, own-build VSIX, `~/.vscode-shared` leak, shell screenshots) | |
| QA-3 | Testing docs in `datapass-vscode-common/testing/` (CODEX_PROCEDURE, AUTO_FORMAT, FEATURES, JOURNEYS, REPORT_FORMAT) + minimal auto (`datapass-vscode-codex-auto`) and audit (`datapass-codex-test`) skeletons; one CODEX_PROCEDURE with "App tests (vsixtest)" and "Client journeys (auto)"; seeds `codex-datapass-vsixtest` (config + QUESTIONS.md V01–V19); 12 §4.7; Codex writes the real journeys | S–M · medium | those repositories' named files (12 §8) | example journeys validate with QA-1's validator; CODEX_PROCEDURE.md uses QA-0's verified commands | TAMPON 22 (reused; restarted after the re-scope) | GO; docs PR #82 (12 §4.6 QA-0 findings). Bridge-type seeds (vsixtest QUESTIONS.md, auto `datapass-auto.json`) are first drafts for the client AI; no native client code | |
| HUB-1 | Hub and common seeds + `scripts/sync-common.ts` | S–M · medium (docs-writer) | `datapass-vscode-hub` README + OPERATING_MANUAL (FR); common README, VERSION, formats/, schemas/, examples/, knowledge/; archive and helper READMEs; common page "How to prepare a client for DataPass" (from guide 06 + onboarding brief); `scripts/sync-common.ts` | sync idempotent, public files only (no handoff/, no FOIL); links resolve (common README, onboarding brief) | TAMPON 18 (reused) | GO + "Working with a client" section (who does what, when, why) in the hub OPERATING_MANUAL and the common page; prerequisite for 1.0.0-rc; on hold: Julian's OK to write the public `datapass-vscode-common` (asked in TAMPON 18) | |
| CQ | Client questions (recurring): answer client `QUESTIONS.md` through common/hub, or in the client's bridge when the answer belongs there; never native client code | S · low | common/hub files, a client bridge | each answer linked from the question; no FOIL/handoff content in common | TAMPON 24 | todo (first pass after HUB-1) | |
| V1-STAB | Stabilisation sweep | M–L · medium (high if > 10 bugs) | fixes in place, each small; a report `handoff/v1/STABILISATION.md` | Report with every testlab step PASS/FAIL; zero unhandled errors in the extension host log for the testlab flows; fixes merged | TAMPON 25 (reserved) | merged (#81): 11 fixes (MCP label, BOM, Readiness by selected variant, Copy Context variant component, no-origin sibling, re-inspect mcp.json, E1 MCP cards, P1 stamps, testlab 10 steps, Workbench Layout in Standard, activeVariant flake); report `handoff/v1/STABILISATION.md`; `windowFlows` flake still open (follow-up #84) | |
| V1-PERF | Activation and refresh budget | M · medium | activation paths in `src/extension.ts` (lazy imports only), a perf fixture and `scripts/perf.ts` | Numbers before/after in the PR; a CI job that fails above 2× the budget | TAMPON 25 (reserved) | running (TAMPON 25) | |
| V1-FOIL-A | FOIL PDF route A runs from DataPass (client side + one DataPass hook if missing) | S · medium | foil-study tasks.json (native repo, no DataPass file), DataPass only if a route is missing | Julian clicks Test on the study component and sees the result with its commit | — | waits for the FOIL AI (client: Codex Wind Lab, then FOIL) and Julian's reviews (FOIL #5/#6, foil-study#1). Design note (ARCHI): the *Test* click records a receipt that sets E1's evidence link "operation verified" for that component and tool — the first real one | |
| V1-PAL | Command palette diet (revises D-03's palette rule) | S–M · medium | package.json `menus.commandPalette` and titles only | A test lists palette-visible commands (snapshot, reviewed); each hidden one has a context menu or view entry; merges **last** before 1.0.0-rc | TAMPON DATAPASSVSCODE - M 2 | todo (GO after V1-PERF and QA-2 merge; merges last, before 1.0.0-rc) | |
| 1.0.0-rc | Release candidate | S · medium | release files | full suites green | — | todo | |

## After 0.23.0 (next packages, not staffed yet)

| Id | Package | Size | Model · effort | Depends on |
|---|---|---|---|---|
| AI-3 | Claude & Codex panel + Codex hand-off (09 §12) | L | Opus 5.5 · high; panel polish medium | Claude Control pass C-1 is merged (claude-control PR #15). Starts after 0.23.0: it shares the AI view and work-order files with the toolkit pass · **Status: merged, released in 0.24.0** (PR #46 + release PR) · **Actual: ≈ 1.2 h · ≈ 18 M tokens** (TAMPON 7, one session, AI-3 + release) |
| AI-4 | Pilot stage 1, read-only (split: AI-4a without sign-in in 0.26, AI-4b qualification with Julian's read-only sign-in later): allowlist design per CLI (`az` + `func` first, for the FOIL PDF flow, 09 §13.1 Q8), `requests/` / `responses/`, Pilot tab | L | allowlist **decided** (xh consult, [brief](briefs/2026-09-26-ai4-pilot-allowlist.md), PR #49: pilot folder with DataPass-written Claude/Codex settings); build: high | toolkit connections (0.18, done); Julian signs in read-only on dev resources · **Status: on hold** (with SUPERVISOR 2, week budget at 71 %) until Julian has run testlab 9 and the panel check and installed `az` / `func` with read-only sign-in |
| FOIL-PDF | Apply the FOIL bridge preparation (foil-v1-vscode-datapass `docs/DATAPASS_PREPARATION.md` steps 1–3), with the A/B/C orchestration variants of 0.25 | M | medium | Q1, Q4, Q5, Q6 answered (see 0.25 inputs); still Q2/Q3 (Julian's Azure names, the two VMs) and the FOIL AI's spike |
| FOIL-WO | First real work order on FOIL (Codex, the heavier Fabric part) | — | — | **standby: Julian says when** |
| 1.0.0 | Release after Julian's acceptance (testlabs 4–9, FOIL on the real repositories, account qualification gate 16) | S | medium | Julian |

## Estimate calibration

Toolkit T1–T4 took ≈ 9 h in one session (estimate 9–14 h): in range. AI-3 + release took ≈ 1.2 h · 18 M tokens against 3–6 h · 60–120 M: 3–5× below. For UI-and-client packages in this repository that reuse existing views and patterns, estimate at a third of the table's size guide; keep the guide for new cores and formats.

## Escalations and decisions

| Date | Package | What happened | Re-run at / consult | Result |
|---|---|---|---|---|
| 2026-09-26 | AI-4 | CLI allowlist design (planned xhigh in 09 §11) | ARCHI xh consult, TAMPON 8 | decided, PR #49, ≈ 0.09 M tokens |
| 2026-09-26 | T1–T4 | a toolkit catalogue session was already running (uncommitted work on 0.20.0) when T1/T2/T4 were staffed | TAMPONs stopped; the running session keeps the packages and rebases | — |
| 2026-09-26 | P1 → main | main red after P1 (#66): its desktop test called `datapass.setActiveVariant`, renamed by 0.25.0 (#65) | ASSISTANT ARCHI fixed the test (#73, rename only) | main green, X1/K2/0.26.0 unblocked |

xhigh consult briefs: `handoff/briefs/<date>-<topic>.md` (question, options, files to read, then "## Decision").

## Open questions for Julian

None blocking. Plan 10 §6 J1–J5 continue with the recommendations (Standard by default, `foil-v1-vscode-datapass` as FOIL's bridge, "bridge repository" wording, J3 and J5 in V2). The pass order toolkit catalogue → AI-3 → AI-4 is Julian's (09 §13.1 Q1).
