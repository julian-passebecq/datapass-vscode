services:
  api:
    build:
      context: ./api
    env_file:
      - api/app.env
      - path: ./missing-but-optional.env
        required: false
  db:
    image: postgres:16
    env_file: ${ENV_FILE}
